@page
@model Kanban_board.Pages.Boards.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this Board?</h3>
<div>
    <h4></h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Board.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Board.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Board.CreatedDate)
        </dt>
        <dd class="col-sm-10">
            @Model.Board.CreatedDate.ToString("yyyy-MM-dd") <!-- Formázott megjelenítés -->
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Board.CreatedBy)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Board.CreatedBy)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Board.BoardId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>